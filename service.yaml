apiVersion: v1
kind: Service
metadata:
  name: register-service
spec:
  selector:
    app: register
  type: NodePort
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30080
